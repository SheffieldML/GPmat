function g = uniformPriorGradient(prior, x)

% UNIFORMPRIORGRADIENT Gradient wrt x of the uniform prior.
%
%	Description:
%	g = uniformPriorGradient(prior, x)
%

% Compute gradient of prior
g = 0 * x;
